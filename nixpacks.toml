[providers]
chromium = "latest"
python = "3.10"

[phases.setup]
nixPkgs = ["chromium", "python310", "python310Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python3 bot.py"
